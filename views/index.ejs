<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input type="text" class="block px-5 py-2 w-full rounded-md outline-none bg-zinc-800" placeholder="Title goes here..." name="title">
                <textarea class="block mt-2 px-5 py-2 w-full rounded-md outline-none resize-none bg-zinc-800" placeholder="Write task details..." name="details"></textarea>
                <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded-md" value="Create Task">
            </form>
        </div>


        <div class="tasks p-10 flex gap-3 flex-wrap">

            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <form action="/delete/<%= val %>" class=" task bg-zinc-800 min-w-72 px-5 py-2 flex-col rounded-md " method="post">
                        <h1 class="text-white text-3xl tracing-tighter"><%= val %></h1>
                        <div class="flex items-center justify-between py-1 px-2">
                            <a href="/file/<%= val %>" class="text-blue-600 inline-block mt-3">Read More..</a>
                            <a href="/file/edit/<%= val %>" class="text-zinc-600 inline-block mt-3">Edit Title..</a>
                        </div>
                        <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 w-full text-white rounded-md" value="Delete Task">
                    </form>
                    
                <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-600">No Task yet...</h3>
            <% } %>

        
        </div>


    </div>
</body>
</html>